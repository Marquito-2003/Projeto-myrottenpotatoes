<h1>All Movies</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <!--
        Os cabeçalhos são agora links.
        - 'link_to' cria o link.
        - 'movies_path' é o helper de rota para a ação 'index'.
        - O hash '{:sort => 'title'}' adiciona '?sort=title' ao URL.
        - ':id' e ':class' são atributos HTML para o link.
      -->
      <th class="<%= @title_header_class %>">
        <%= link_to 'Movie Title', movies_path(:sort => 'title'), :id => 'title_header' %>
      </th>
      <th>Rating</th>
      <th class="<%= @release_date_header_class %>">
        <%= link_to 'Release Date', movies_path(:sort => 'release_date'), :id => 'release_date_header' %>
      </th>
      <th>More Info</th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.rating %></td>
        <td><%= movie.release_date.strftime('%F') %></td>
        <td><%= link_to "More Info", movie_path(movie) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary' %>